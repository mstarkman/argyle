<% if @sentinal.persisted? %>
  window.Argyle.modal.close();
  $("#sentinal-list").replaceWith("<%= j render "sentinal_list", sentinals: @sentinals %>");
<% else %>
  window.Argyle.modal.replaceForm("<%=j render "new_form", sentinal: @sentinal %>");
<% end %>